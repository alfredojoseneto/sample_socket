[tool.poetry]
name = "socket-example"
version = "0.1.0"
description = ""
authors = ["Alfredo Neto"]
readme = ""

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
